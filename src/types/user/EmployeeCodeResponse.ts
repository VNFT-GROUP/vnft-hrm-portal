export interface EmployeeCodeResponse {
  id: string;
  prefix: string;
  description?: string;
  active: boolean;
  createdAt: string;
  updatedAt: string;
  createdBy: string;
  updatedBy: string;
}
